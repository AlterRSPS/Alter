package org.alter.scrapper.datascrapper.item

import org.alter.scrapper.Scrapper
import java.nio.file.Path

class ItemLoader : Scrapper {
    override fun init(output: Path) {
        init(output)
    }

    override fun postLoad() {

    }
}